<table class="table table-striped table-hover">
  <thead>
    <tr class="active">
      <th><%= sortable "name", "Nazwa" %></th>
      <th>SN</th>
      <th>Ilość</th>
	  <th>Jednostka</th>
	  <th><%= sortable "regal", "Regał"%></th>
	  <th>Półka</th>
      <th>Uwagi</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @explos.each do |explo| %>
      <tr>
        <td><%= explo.name.truncate(30) %></td>
        <td><%= explo.sn.truncate(20) %></td>
        <td><%= explo.quantity %></td>
		<td><%= explo.unit.name %></td>
		<td><%= explo.regal %></td>
		<td><%= explo.polka %></td>
        <td><%= explo.description.truncate(10) %></td>
		<td style="width:80px"> <%= link_to "Więcej", explo_path(explo) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= will_paginate @explos%>