<h2>Dodanie zdjęcia</h2>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@item) do |f| %>
	  
 	 	<div class="form-group">
        <%= f.label :avatar, class: 'col-sm-2 control-label'  %>
        <div class="col-sm-6">
            <%= f.file_field :avatar %>
        </div>
	  </div>
      <%= f.submit "Aktualizuj", class: "btn btn-primary" %>
    <% end %>

  </div>
</div>

	
	