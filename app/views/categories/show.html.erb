
<div class="page-header">
  <h1>Item</h1>
</div>

<dl class="dl-horizontal">
  <dt><strong>ID</strong></dt>
  <dd><%= @category.id %></dd>
  <dt><strong>Nazwa</strong></dt>
  <dd><%= @category.name %></dd>
  <dt><strong>Uwagi</strong></dt>
  <dd><%= @category.description %></dd>
  
  <br>


<div>
<table class="table table-striped table-hover">
  <thead>
    <tr class="active">
      <th>Nazwa</th>
      <th>SN</th>
      <th>Sala</th>
      <th>Zawód</th>
      <th>Miasto</th>
      <th>Ilość</th>
      <th>Uwagi</th>
      <th>Kategoria</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= item.name.truncate(30) %></td>
        <td><%= item.sn.truncate(20) %></td>
        <td><%= item.hall %></td>
        <td><%= item.proffesion %></td>
        <td><%= item.city %></td>
        <td><%= item.quantity %></td>
        <td><%= item.description.truncate(10) %></td>
        <td><%= item.category.name %></td>
			  <td style="width:80px"> <%= link_to image_tag("http://icons.iconarchive.com/icons/double-j-design/super-mono-3d/96/button-round-play-icon.png",:width=>30,:height=>30, :border=>0), item_path(item) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
  <%= link_to image_tag("http://icons.iconarchive.com/icons/double-j-design/super-mono-3d/96/button-round-fast-backward-icon.png",:width=>50,:height=>50, :border=>0), categories_path %> 
<%= will_paginate @items %>
</div>
